{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing scoped to domain primitives. Start narrow; expand to src/rules once baseline is stable. Coverage gate prevents weak tests from masking regressions.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.stryker.config.ts"
  },
  "checkers": [],
  "reporters": ["progress", "clear-text", "html"],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "_mutate_comment": "Mutate domain primitives + rules with isolated unit tests. CommandSafety, ContentAppropriateness, ContentOrganization, FileLocation, ImportResolution, ImportSyntax, MonorepoHierarchy currently lack unit tests — adding mutation testing on them produces 100% no-coverage noise. Add a unit test file before extending this list.",
  "mutate": [
    "src/domain/ContextFile.ts",
    "src/domain/CustomRule.ts",
    "src/domain/LintingResult.ts",
    "src/domain/Location.ts",
    "src/domain/RulesEngine.ts",
    "src/domain/Severity.ts",
    "src/domain/Violation.ts",
    "src/rules/CodeBlockRule.ts",
    "src/rules/CommandSafetyRule.ts",
    "src/rules/ContentOrganizationRule.ts",
    "src/rules/ContentRule.ts",
    "src/rules/FileLocationRule.ts",
    "src/rules/FileSizeRule.ts",
    "src/rules/FormatRule.ts",
    "src/rules/HookConfigurationRule.ts",
    "src/rules/ImportResolutionRule.ts",
    "src/rules/ImportSyntaxRule.ts",
    "src/rules/SkillStructureRule.ts",
    "src/rules/StructureRule.ts",
    "src/rules/SubagentStructureRule.ts"
  ],
  "ignorePatterns": ["dist", "node_modules", "reports"],
  "_thresholds_comment": "Threshold tuned to current rule-layer baseline (~58%). Domain alone is at 92%; rules drag the average down. Tighten as rule tests are strengthened.",
  "thresholds": {
    "high": 80,
    "low": 60,
    "break": 45
  },
  "concurrency": 4,
  "timeoutMS": 15000,
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true
}
