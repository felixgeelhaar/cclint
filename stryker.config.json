{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing scoped to domain primitives. Start narrow; expand to src/rules once baseline is stable. Coverage gate prevents weak tests from masking regressions.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.stryker.config.ts"
  },
  "checkers": [],
  "reporters": ["progress", "clear-text", "html"],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "_mutate_comment": "Only mutate files whose tests live in tests/unit/domain/. CodeBlock and Config are exercised by rule and infrastructure tests; expand scope when those tests are added to the Stryker run config.",
  "mutate": [
    "src/domain/ContextFile.ts",
    "src/domain/CustomRule.ts",
    "src/domain/LintingResult.ts",
    "src/domain/Location.ts",
    "src/domain/RulesEngine.ts",
    "src/domain/Severity.ts",
    "src/domain/Violation.ts"
  ],
  "ignorePatterns": ["dist", "node_modules", "reports"],
  "thresholds": {
    "high": 80,
    "low": 60,
    "break": 50
  },
  "concurrency": 4,
  "timeoutMS": 15000,
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true
}
