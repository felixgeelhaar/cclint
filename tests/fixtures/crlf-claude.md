# Project

## Overview

This file uses Windows (CRLF) line endings.
Every line ends with a carriage return + newline.

## Notes

- No line has real trailing whitespace.
- The only carriage returns come from the CRLF endings.
